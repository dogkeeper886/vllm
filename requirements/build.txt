# Should be mirrored in pyproject.toml
cmake>=3.26.1
ninja
packaging>=24.2
setuptools>=77.0.3,<80.0.0
setuptools-scm>=8
torch==1.12.1  # Modified for Tesla K80 compatibility (was 2.7.1) - use base version, CUDA will be handled at runtime
wheel
jinja2>=3.1.6
regex
